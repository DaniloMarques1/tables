
  <%# <div id="items"> %>
    <%# <%= fields_for :items do |items| %>
      <%# <%= render partial: "items", locals: {f:items} %>
    <%# <%= link_to_association('[Novo Item]', form, :items)%>
  <%# </div> %> 
  <%= form_for :item do |f| %>
  <div class="nested-fields">
    <div class="field">
      <%= f.label :number %>
      <%= f.text_field :number %>

      <%= f.label :description %>
      <%= f.text_field :description %>
      <%# <%= link_to_remove_association('[Remover]', f) %> 
    </div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
   <%# <%= f.submit %>
<% end %>


  
